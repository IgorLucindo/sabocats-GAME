PHASES:
    - Phase 1: Game Loops & Systems (is it good and robust already, given that you already worked on it, or is there something missing?)
    - Phase 2: UI & Menu System
    - Phase 3: Class Design 1
        classes folder should be gone
        classes files inside class folder should be integrated into already existing files or other more appropriate destiny
    - Phase 4: Class Design 2
        have appropriate classes and classes names for (objects, background, player, ...)
        classes could be rewritten so they are more robust (like box, object, player, selectable player, online player, ... All of them)
        add class for each object (is it the most appropriate approach? if not, don't do it. And if so, should we delete object data files?)
        add class for map voting
        map class with all background and collision blocks (I just want the most robust approach for class map)
    - Phase 5: Utils redesign
        right now it works more like a dumper, but it shouldn't be like that
    - Phase 6: Data redesign
        is it more appropriate to have data files as json files and load them?

    - Phase 7: Robust State Machine
        Add validation to MatchStateMachine:
        Create TransitionRules system (define valid state paths)
        Validate transitions (prevent choosing→scoreboard directly)
        Add state-specific transition guards
        Better error messages
        Result: Impossible to reach invalid game states
    - Phase 8: Event-Driven Networking
        Replace direct socket calls with EventBus:
        Create GameEvents system (define all game events)
        Map socket messages to events
        Systems react to events instead of direct calls
        Decouple networking from game logic
        Result: All state changes flow through EventBus
    - Phase 9: Performance Optimization
        Profile and optimize:
        Implement profiler (show FPS + frame time breakdown)
        Identify bottlenecks (collision detection, rendering, etc)
        Optimize top 3 slowest systems
        Implement object pooling if needed
        Consider spatial partitioning for collisions
        Result: 30-60 FPS stable, documented bottlenecks
        right now the code is not heavy to be honest, but keeping the code well optimized is always a good idea



CODE ARCHITECTURE:
    fix:
    - parallax stopped working. fix.
    - have better implementation of map voting or is this unecessary? if it is, then do it for a cleaner code.
    - would it be robust, cleaner and more appropriate to have events being staticly defined, instead of dynamically defining them? or not?
    - have more robust pub and sub for more modular communication between client and server. (I want the most appropriate implementation for these multiplayer things)
    - upload all sprites with correct scale, and after remove scale settings for sprites (I don't want scale because it mess up with pixel art resolutions)

    add:
    - use the defined events, or create new ones, like 'player:loaded' for a more robust and modular code. what do you think about that?
    - send to server functions all together in a single function at the end of the gameloop (I just want the most robust approach for pub)
    - set ctx.imageSmoothingEnabled to true for zomming camera, false otherwise (I don't remember what is this for or what does this do)



GAMEPLAY:
    debug:
    - add a debug menu or something that allows to explore different parts of the game, without breaking anything, of course.

    fix:
    - dead player being rendered even in choosing and placing phase. lets unload them in start of choosing or end of scoreboard, whatever is more appropriate
    - placed objects not showing up for other users
    - sync spawn point of players (create a seed for that)
    - wrong point calculation in scoreboard (showing different results for each user)
    - use interpolation in renderLoop (what I want actually is a smooth online player movement. idk if that is the best approach for that)

    add:
    - grid background generated dynamically
    - lobby gui with players' icons
    - press to start game screen. This would automatically create a new distinct room with only you.
    The room has an id so other players can join your room.
    Is it fine to have room data handled by the socket server, or would it be better to store the room data in a dataset like fireabse? not many people will play the game anyway, but you know better.
    - room systems (start)
    - box objects: 
        dog  (1 per match)
        item seller (sells items for players)
        hacking tool (turn closer objects crazier, but with a downside)
    - skins and accessories for cats
    - improve lore
    - allow to upload a object pack with custom objects that will be loaded and added to the game by uploading a .zip file with the objects data and assets. object pack for your room and everybody that will join your room. if using firebase, can this pack be stored in the firebase database (.json and images)?

    redo:
    - cat sprite
    - box objects sprites
    - map design and background